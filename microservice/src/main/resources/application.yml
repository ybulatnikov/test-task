server:
  port: 8081

spring:
  application:
    name: microservice-app

eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka/

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"
